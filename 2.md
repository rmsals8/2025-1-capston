데이터베이스 설계를 위해 필요한 주요 테이블과 필드들을 추천해드리겠습니다:

Users 테이블 (기본 사용자 정보)


user_id (PK)
email
password (암호화)
name
phone_number
created_at
updated_at
last_login_at
is_active
profile_image_url


SocialLogins 테이블 (소셜 로그인 연동)


social_login_id (PK)
user_id (FK)
provider_type (KAKAO, NAVER 등)
provider_user_id
access_token
refresh_token
token_expires_at


VisitHistory 테이블 (방문 기록)


visit_id (PK)
user_id (FK)
place_id
place_name
place_category
visit_date
rating (선택적 평점)
review (선택적 리뷰)
latitude
longitude


UserPreferences 테이블 (사용자 선호도)


preference_id (PK)
user_id (FK)
category_id (FK)
preference_level (1-5)
updated_at


Categories 테이블 (장소 카테고리)


category_id (PK)
category_name
parent_category_id (계층구조를 위한)
description


SavedPlaces 테이블 (찜한 장소)


saved_place_id (PK)
user_id (FK)
place_id
place_name
place_category
saved_at
visit_planned_date (선택적)
latitude
longitude


UserRecommendations 테이블 (추천 기록)


recommendation_id (PK)
user_id (FK)
place_id
recommendation_type (SIMILAR, CATEGORY_BASED 등)
recommended_at
is_visited
recommendation_score


PasswordResets 테이블 (비밀번호 재설정)


reset_id (PK)
user_id (FK)
token
expires_at
is_used

추가 고려사항:

모든 테이블에 soft delete를 위한 deleted_at 컬럼 추가 고려
위치 데이터를 위한 공간 인덱스 사용
카테고리는 계층구조 지원을 위한 설계 필요
방문 기록과 추천을 위한 인덱스 최적화
소셜 로그인 토큰 관리를 위한 보안 설계
사용자 선호도 분석을 위한 집계 테이블 고려